import { PhasePlaceholder } from '../../components/layout/PhasePlaceholder';

export function SuperConnectorPage() {
  return (
    <PhasePlaceholder
      title="SuperConnector"
      phase="Phase 4 (registration) and Phase 5 (matching engine)"
    />
  );
}
