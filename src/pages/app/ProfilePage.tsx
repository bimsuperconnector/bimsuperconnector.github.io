import { PhasePlaceholder } from '../../components/layout/PhasePlaceholder';

export function ProfilePage() {
  return <PhasePlaceholder title="Profile" phase="Phase 2 (Profiles & batch management)" />;
}
